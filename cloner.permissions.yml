access all entity cloner:
  title: 'Access to any cloner form.'
  description: 'Bypass all checks for every form clone.'
  restrict access: true

permission_callbacks:
  - Drupal\cloner\ClonerDynamicPermissions::permissions
